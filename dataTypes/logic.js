// in java Script we have two data types 'Primitive' and 'Non premetive' data types

//Primitive data types
//----->Primitive data types is used for store small values, they are in-muteable,and etc<------//
//1. Strings 2.Numbers 3.boolean 4.Undefine 5.Null 6.Symbol

const PersonOne = 'saleem khan';
const numOne = 3;
const conditionOne = true;
let udef ;
const nulla =null;
const symb = Symbol('go');


console.log(PersonOne);
console.log(numOne);
console.log(conditionOne);
console.log(udef);
console.log(nulla);

//------------>Characteristics of primitive<-----------//
//1.Immutable :primitive data type can't be changed onece they are created

//--------->Important note <---------//
//. In js string are premitive data type but the they have some object like behavior such as suppoerting medthod like toUpperCase() and toLowerCase() indluding one peroperty like length

//more details are 
// string concatination of string and numbers.
// number comperision / number with strings
// number arethematic operators
// control flow statements
// compeision operaters